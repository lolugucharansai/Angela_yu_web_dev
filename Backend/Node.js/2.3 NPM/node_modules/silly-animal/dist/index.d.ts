import { adjectives, animals } from './dicts';
declare const randomItem: <T>(arr: T[]) => T;
declare function generate(divider?: string): string;
export { adjectives, animals, generate, randomItem };
